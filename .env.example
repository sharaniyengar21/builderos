DATABASE_URL="postgresql://builderos:builderos@localhost:5432/builderos"
REDIS_URL="redis://localhost:6379"

# generate both with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SESSION_SECRET=""
CREDENTIAL_ENCRYPTION_KEY=""

# the self-hoster's own account; log in with this email + password at /login
OWNER_EMAIL="you@example.com"
OWNER_PASSWORD="change-me"

NEXT_PUBLIC_APP_URL="http://localhost:3000"
